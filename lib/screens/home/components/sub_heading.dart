import 'package:flutter/material.dart';

class SubHeading extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text(
      "Not sure what to eat? Press the button below.",
      style: TextStyle(fontSize: 16, color: Colors.black),
    );
  }
}
